# 1Password Configuration Snippet
# This file is sourced by zshrc if security module is enabled

if command -v op &> /dev/null; then
    # Enable 1Password CLI plugins
    # source <(op completion zsh)
    :
fi
