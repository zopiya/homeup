# Homeup 速查卡片 (Quick Reference)

> 常用命令、快捷键和操作快速参考。打印或收藏这个文档！

---

## 📋 Just 常用命令速查

### 基础命令

```bash
just bootstrap        # 完整初始化（包装吃、配置、所有设置）
just apply            # 应用Chezmoi配置（不安装包）
just install          # 安装包（Homebrew）
just doctor           # 系统诊断检查
just help             # 显示所有可用命令
```

### 包管理

```bash
just add-pkg brew:curl            # 添加包到Brewfile.core
just remove-pkg curl              # 从Brewfile中移除包
just update-packages              # 更新所有包
just validate-brewfile            # 验证Brewfile语法
just clean-homebrew               # 清理Homebrew缓存
```

### 配置管理

```bash
just init                         # 初始化Chezmoi
just update-config                # 更新Chezmoi配置
just diff                         # 显示配置变化
just apply-config                 # 应用配置更改
```

### 验证和测试

```bash
just validate-all                 # 运行所有验证
just test-bootstrap               # 测试bootstrap脚本
just check-profile                # 检查当前profile
just lint-yaml                    # YAML语法检查
```

### Shell命令

```bash
just shell                        # 进入Just shell
:quit                             # 退出Just shell（在shell中）
```

---

## 🔧 Chezmoi 速查

### 基本操作

```bash
chezmoi init --source . --apply   # 初始化并应用
chezmoi apply                     # 应用配置（同步到主目录）
chezmoi diff                      # 显示配置变化
chezmoi add ~/.zshrc              # 添加文件到管理
chezmoi update                    # 从源拉取并应用
```

### 编辑和管理

```bash
chezmoi edit ~/.zshrc             # 编辑托管文件
chezmoi remove ~/.oldfile         # 停止管理文件
chezmoi status                    # 显示配置状态
chezmoi dump                      # 导出所有配置为JSON
```

### 调试和诊断

```bash
chezmoi doctor                    # 诊断常见问题
chezmoi verify                    # 验证配置完整性
chezmoi show ~/.zshrc             # 显示文件内容
```

---

## 🖊️ Neovim 快捷键速查

### 基本导航

| 快捷键 | 功能 | 快捷键 | 功能 |
|--------|------|--------|------|
| `h` | 左移 | `j` | 下移 |
| `k` | 上移 | `l` | 右移 |
| `w` | 下一个单词 | `b` | 上一个单词 |
| `$` | 行末 | `^` 或 `0` | 行首 |
| `gg` | 文件开头 | `G` | 文件结尾 |
| `Ctrl+D` | 向下翻页 | `Ctrl+U` | 向上翻页 |

### 编辑模式

| 快捷键 | 功能 | 快捷键 | 功能 |
|--------|------|--------|------|
| `i` | 插入模式 | `o` | 新行插入 |
| `a` | 追加 | `O` | 上一行插入 |
| `v` | 字符选择 | `V` | 行选择 |
| `Ctrl+V` | 块选择 | `u` | 撤销 |
| `Ctrl+R` | 重做 | `dd` | 删除行 |

### 查找和替换

```
/pattern              搜索正向
?pattern              搜索反向
n                     下一个匹配
N                     上一个匹配
:%s/old/new/g         全文替换
:20,30s/old/new/g     指定行替换
```

### LSP操作

```
gd                    跳转到定义
gr                    查看引用
K                     显示文档
<space>ca             代码操作
<space>rn             重命名
<space>f              格式化
```

### 分窗和标签页

```
:split (or :sp)       水平分割
:vsplit (or :vs)      垂直分割
Ctrl+W + h/j/k/l      切换窗口
Ctrl+W + =            平衡窗口
:tabnew               新标签页
gt                    下一个标签页
gT                    上一个标签页
```

---

## 🐚 Shell 快捷键速查

### Zsh 快捷键

| 快捷键 | 功能 | 快捷键 | 功能 |
|--------|------|--------|------|
| `Ctrl+A` | 行首 | `Ctrl+E` | 行末 |
| `Ctrl+L` | 清屏 | `Ctrl+C` | 中断 |
| `Ctrl+D` | 退出 | `Ctrl+Z` | 暂停 |
| `Ctrl+R` | 反向搜索历史 | `Ctrl+S` | 正向搜索历史 |
| `Alt+B` | 上一个单词 | `Alt+F` | 下一个单词 |
| `Ctrl+K` | 删除光标后内容 | `Ctrl+U` | 删除光标前内容 |

### 常用别名（来自aliases.zsh）

```bash
cd ..                 # go up one level
...                   # go up three levels
....                  # go up four levels
~                     # home directory
-                     # previous directory
ls                    # list files (colorized)
ll                    # long listing
la                    # list all including dotfiles
gr                    # grep recursive
less                  # pagination with colors
```

### 常用函数

```bash
mkcd <dir>            # 创建目录并进入
extract <file>        # 自动解压文件
weather <city>        # 获取天气信息
bak <file>            # 备份文件
```

---

## 🔑 Git + SSH 速查

### Git 配置

```bash
git config user.name "Your Name"           # 设置用户名
git config user.email "you@example.com"    # 设置邮箱
git config user.signingkey ~/.ssh/id       # SSH签名密钥
git config commit.gpgsign true             # 启用提交签名
```

### SSH 密钥管理

```bash
ssh-keygen -t ed25519-sk -C "comment"      # 生成YubiKey密钥
ssh-keygen -t ed25519 -C "comment"         # 生成本地密钥
ssh-add -K ~/.ssh/id_rsa                   # 加载密钥
ssh-add -L                                  # 列出已加载密钥
ssh-keyscan github.com >> ~/.ssh/known_hosts
```

### Git 常用命令

```bash
git add <file>                   # 暂存文件
git commit -m "message"          # 提交（自动签名）
git push                         # 推送到远程
git pull                         # 拉取更新
git status                       # 显示状态
git log --oneline -10            # 查看最近10个提交
git diff                         # 显示未暂存变化
git diff --staged                # 显示已暂存变化
```

### Lazygit TUI

```
j/k                  上下移动
l/h                  右左
o                    打开
Space                选择
c                    提交
p                    推送
d                    删除
m                    标记
r                    变基
```

---

## 💻 Tmux 快捷键速查

### 前缀键

```
Ctrl+B              前缀键（默认）
:                   命令模式
?                   显示快捷键
```

### 会话管理

```bash
tmux new-session -s name          # 创建会话
tmux attach -t name               # 附加到会话
tmux kill-session -t name         # 删除会话
tmux list-sessions                # 列出会话
```

### 窗口操作

| 快捷键 | 功能 |
|--------|------|
| `Prefix+c` | 新建窗口 |
| `Prefix+,` | 重命名窗口 |
| `Prefix+n` | 下一个窗口 |
| `Prefix+p` | 上一个窗口 |
| `Prefix+&` | 关闭窗口 |

### 窗格操作

| 快捷键 | 功能 |
|--------|------|
| `Prefix+"` | 水平分割 |
| `Prefix+%` | 垂直分割 |
| `Prefix+↑/↓/←/→` | 导航窗格 |
| `Prefix+o` | 切换活动窗格 |
| `Prefix+x` | 关闭窗格 |
| `Prefix+z` | 最大化窗格 |

### 复制模式

```
Prefix+[            进入复制模式
Space               开始选择
Enter               复制
Prefix+]            粘贴
```

---

## 🎯 Zellij 快捷键速查（Tmux兼容模式）

### 基本操作

```
Ctrl+A              进入Tmux模式
Ctrl+A c            新建标签页
Ctrl+A "            水平分割
Ctrl+A %            垂直分割
Ctrl+A h/j/k/l      导航窗格
Ctrl+A x            关闭窗格
Ctrl+A d            分离会话
```

### 原生快捷键（更快）

```
Alt+1-9             切换标签页
Alt+N               新窗格
Alt+H/J/K/L         导航
Alt+=/+             增大窗格
Alt+-               缩小窗格
```

---

## 📦 Homebrew 速查

### 基本操作

```bash
brew install pkg                  # 安装包
brew uninstall pkg                # 卸载包
brew upgrade pkg                  # 升级包
brew upgrade                      # 升级所有包
brew list                         # 列出已安装包
brew search keyword               # 搜索包
```

### Bundle管理

```bash
brew bundle dump                  # 导出当前包到Brewfile
brew bundle install               # 安装Brewfile中的包
brew bundle cleanup --force       # 卸载不在Brewfile中的包
brew bundle check --verbose       # 检查一致性
```

### 维护

```bash
brew doctor                       # 诊断问题
brew update                       # 更新Homebrew
brew cleanup                      # 清理旧包
brew cache                        # 显示缓存位置
```

---

## 🎛️ 其他工具速查

### Mise 版本管理

```bash
mise install node@20              # 安装特定版本
mise list                         # 列出已安装工具
mise use node@20                  # 激活版本
mise ls-remote node               # 列出可用版本
```

### Direnv 环境管理

```bash
direnv allow                      # 允许.envrc
direnv deny                       # 拒绝.envrc
direnv status                     # 显示状态
direnv edit                       # 编辑.envrc
```

### Lefthook Git钩子

```bash
lefthook run pre-commit           # 手动运行pre-commit钩子
lefthook uninstall                # 卸载所有钩子
lefthook install                  # 安装所有钩子
```

---

## 🔐 YubiKey 速查

### 列出YubiKey信息

```bash
ykman info                        # 显示YubiKey信息
ykman openpgp info                # OpenPGP应用信息
```

### SSH密钥操作

```bash
ssh-keygen -t ed25519-sk          # 生成SSH密钥
ssh-keygen -y -f ~/.ssh/id_ed25519_sk > ~/.ssh/id_ed25519_sk.pub
ssh-add -s /usr/local/lib/libykcs11.dylib  # 添加PKCS11库
```

### 验证SSH签名

```bash
ssh-keygen -Y check-novalidate -n file -s sig -f key.pub
```

---

## 📊 诊断和调试速查

### 系统诊断

```bash
just doctor                       # Homeup系统检查
uname -a                          # 系统信息
echo $SHELL                       # 当前shell
$SHELL --version                  # Shell版本
```

### 路径和配置

```bash
echo $PATH                        # 显示PATH
echo $ZDOTDIR                     # Zsh配置目录
which <cmd>                       # 显示命令路径
type <cmd>                        # 显示命令类型
```

### 性能分析

```bash
time zsh -i -c exit               # Zsh启动时间
time nvim -c exit                 # Neovim启动时间
brew list | wc -l                 # 包数量
```

### 日志位置

```
~/.local/share/chezmoi/           Chezmoi源目录
~/.config/zsh/                    Zsh配置
~/.config/nvim/                   Neovim配置
~/.ssh/                           SSH配置和密钥
```

---

## 🎓 常见工作流组合

### 日常开发

```bash
# 早上
just doctor                       # 检查系统状态
git pull                          # 拉取最新更改
direnv allow                      # 激活项目环境

# 开发中
nvim <file>                       # 编辑代码
git add .                         # 暂存更改
git commit -m "..."               # 提交（自动签名）
git push                          # 推送

# 跨机器同步
chezmoi update                    # 应用最新配置
just update-packages              # 更新包
```

### 新机器设置

```bash
# 1. 初始化
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
git clone https://github.com/zopiya/homeup.git
cd homeup

# 2. 完整启动
just bootstrap

# 3. 验证
just doctor

# 4. 个性化
chezmoi edit ~/.zshrc
just update-config
```

### 故障排查

```bash
# 1. 检查状态
just doctor
chezmoi status
git status

# 2. 查看详细信息
chezmoi diff
git log --oneline -10

# 3. 应用修复
chezmoi apply
just apply
just validate-all
```

---

## 🔗 相关资源

- **完整文档**: `docs/` 目录
- **快速导航**: `docs/00_快速导航指南.md`
- **故障排查**: `docs/51_故障排查与常见问题.md`
- **配置文件**: `~/.config/` 各工具配置目录

---

## 💡 提示

- 用 `Ctrl+F` (Cmd+F) 搜索快捷键
- 定期打印此文档作为快速参考
- 将最常用的命令设为alias
- 使用 `just` 前面加 `:` 进入交互模式

**版本**: 2.1 | **最后更新**: 2024-01-16
